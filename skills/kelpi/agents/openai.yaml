interface:
  display_name: "Kelpi"
  short_description: "Set up Kelpi MCP and first flow."
  default_prompt: "Use $kelpi to connect Kelpi MCP, verify the workspace, and onboard the first live flow."
dependencies:
  tools:
    - type: "mcp"
      value: "kelpi"
      description: "Kelpi hosted MCP server"
      transport: "streamable_http"
      url: "https://app.kelpi.ai/api/mcp"
policy:
  allow_implicit_invocation: true
