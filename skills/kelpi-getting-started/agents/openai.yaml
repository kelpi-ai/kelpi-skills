interface:
  display_name: "Kelpi Getting Started"
  short_description: "Launch first sender, flow, and SDK."
  default_prompt: "Use $kelpi-getting-started to onboard a new Kelpi user from workspace check through first live flow."
dependencies:
  tools:
    - type: "mcp"
      value: "kelpi"
      description: "Kelpi hosted MCP server"
      transport: "streamable_http"
      url: "https://app.kelpi.ai/api/mcp"
policy:
  allow_implicit_invocation: true
